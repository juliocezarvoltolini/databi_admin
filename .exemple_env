# Database
DATABASE_URL="postgresql://usuario:senha@localhost:5432/databi_admin"

# JWT
JWT_SECRET="seu-jwt-secret-muito-seguro-aqui"

# SMTP Configuration for Email Verification
SMTP_HOST=email-ssl.com.br
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=admin@databi-ge.com.br
SMTP_PASS=sua-senha
SMTP_FROM_NAME="Data BI"
SMTP_FROM_EMAIL=admin@databi-ge.com.br

NEXT_PUBLIC_BASE_URL=http://localhost:3001

# Alternativa com email-ssl.com.br (hostname do certificado)
# SMTP_HOST=email-ssl.com.br
# SMTP_PORT=587
# SMTP_SECURE=false